CONFIGDIR = $(GLOBAL_CONFIGDIR)

include $(CONFIGDIR)/config.mk

COPY_TREE = ./files

MKI_PACK_RESULTS = $(GLOBAL_TEST_MKI_PACK_RESULTS)

ifdef GLOBAL_TEST_MKI_TAR_COMPRESS
	MKI_TAR_COMPRESS = $(GLOBAL_TEST_MKI_TAR_COMPRESS)
endif

ifdef GLOBAL_TEST_MKI_CPIO_COMPRESS
	MKI_CPIO_COMPRESS = $(GLOBAL_TEST_MKI_CPIO_COMPRESS)
endif

include $(CONFIGDIR)/targets.mk

all: copy-tree run-scripts pack-image
